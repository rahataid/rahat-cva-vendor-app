import RefreshAccessToken from "@sections/auth/refresh-access-token";
import "../theme/title.css";
import { FC } from "react";

const RefreshAccessTokenPage: FC = () => {
  return <RefreshAccessToken />;
};

export default RefreshAccessTokenPage;
